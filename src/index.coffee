module.exports = {Data, Either, Enum, Maybe, Bool, Number, String, Dict, Strict, Any, match}

{Number, String, Bool, Any, Enum, Maybe, Either, Dict, Strict, Data} = require './primitives'
{match} = require './match'

